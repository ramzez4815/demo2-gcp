apiVersion: v1
kind: Service
metadata:
  name: ghost
spec:
  selector:
    app: ghost
  ports:
  - port: 80
    targetPort: 2368 # exposed port of the ghost container
  type: LoadBalancer
  loadBalancerSourceRanges:
  - 187.232.104.210/32